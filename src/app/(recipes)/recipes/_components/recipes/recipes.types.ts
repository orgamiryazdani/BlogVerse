export type RecipesCardProps = {
    id: number
    name: string
    image: string
    rating: number
    reviewCount: number
    difficulty: string
    prepTimeMinutes: number
    cookTimeMinutes: number
    cuisine: string
}
